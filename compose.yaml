services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: inter-java-challenge:local
    ports:
      - "8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=75.0"
    restart: unless-stopped
